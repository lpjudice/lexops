# Mac service dependencies — runs locally on the Mac, NOT deployed to Fly.io.
# The collection logic (buscar_via_pdpj, etc.) is imported directly from
# ../app/services/consulta_processual/ — same repo, same Python path.
# Keep in sync with ../requirements.txt for shared deps (httpx version, etc.).

aiogram==3.21.0
playwright==1.54.0
httpx==0.28.1
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
